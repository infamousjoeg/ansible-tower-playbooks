- hosts: localhost

  tasks:
    # Display the secret value returned as the password of a Machine credential
    - name: Display Secret Value
      debug:
        msg:
          - "Secret Value: {{ ansible_ssh_pass }}"
